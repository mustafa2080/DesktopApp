# إصلاح خطأ "value ('0') must be less than '0'" في دفعات فواتيرك

## 📋 وصف المشكلة

عند محاولة تعديل دفعة من فواتيرك، كان يظهر الخطأ التالي:
```
خطأ في تعديل الدفعة: value ('0') must be less than '0'. (Parameter 'value')
Actual value was 0.
```

## 🔍 سبب المشكلة

المشكلة كانت في ملف `FawateerkPaymentsReportForm.cs` في دالة `EditPayment()`.

**الكود الخاطئ:**
```csharp
NumericUpDown numAmount = new NumericUpDown();
numAmount.Maximum = 1000000000m;
numAmount.DecimalPlaces = 2;
numAmount.Value = Math.Max(0.01m, payment.Amount);  // ❌ تعيين Value قبل Minimum
numAmount.Minimum = 0.01m;  // ❌ تعيين Minimum بعد Value
```

**المشكلة:** عندما يكون `payment.Amount = 0` أو أقل من `0.01`، يحاول الكود تعيين `Value = 0.01` لكن الـ `Minimum` الافتراضي لـ `NumericUpDown` هو `0`، مما يسبب exception لأن القيمة أكبر من الحد الأدنى الافتراضي.

## ✅ الحل

تم إصلاح المشكلة بتعيين `Minimum` **قبل** تعيين `Value`:

**الكود الصحيح:**
```csharp
NumericUpDown numAmount = new NumericUpDown();
numAmount.Location = new Point(150, 30);
numAmount.Size = new Size(280, 30);
numAmount.Minimum = 0.01m;  // ✅ تعيين Minimum أولاً
numAmount.Maximum = 1000000000m;
numAmount.DecimalPlaces = 2;
numAmount.Font = new Font("Cairo", 10F);
numAmount.Value = Math.Max(0.01m, payment.Amount <= 0 ? 100m : payment.Amount);  // ✅ تعيين Value ثانياً
```

## 📁 الملفات المُعدَّلة

1. **`Presentation/Forms/FawateerkPaymentsReportForm.cs`** (السطر 890)
   - تم تعديل ترتيب تعيين خصائص `NumericUpDown`

## 🔧 التحقق من قاعدة البيانات

تم إنشاء ملف SQL للتحقق من وجود دفعات بمبلغ صفر:
- `fix_fawateerk_zero_amount.sql`

قم بتشغيل هذا الملف للتأكد من عدم وجود بيانات خاطئة في قاعدة البيانات.

## ✨ النتيجة

الآن يمكن تعديل دفعات فواتيرك بدون مشاكل، حتى لو كان المبلغ الأصلي صفر أو قريب من الصفر.

## 📝 ملاحظات مهمة

1. **قاعدة عامة:** عند استخدام `NumericUpDown`، يجب دائماً تعيين `Minimum` و `Maximum` **قبل** تعيين `Value`.
2. **ترتيب الخصائص المُوصى به:**
   ```csharp
   var num = new NumericUpDown();
   num.Minimum = ...;      // 1️⃣ أولاً
   num.Maximum = ...;      // 2️⃣ ثانياً
   num.DecimalPlaces = ...; // 3️⃣ ثالثاً
   num.Value = ...;        // 4️⃣ أخيراً
   ```

## 🚀 خطوات الاختبار

1. قم ببناء المشروع (Build)
2. افتح تقرير دفعات فواتيرك
3. اختر أي دفعة واضغط على "تعديل"
4. يجب أن تظهر نافذة التعديل بدون أخطاء
5. جرب تعديل المبلغ والحفظ

---

**تاريخ الإصلاح:** 14 فبراير 2026
**الحالة:** ✅ تم الحل بنجاح
